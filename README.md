# php-reverse-shell
 This script will make an outbound TCP connection to a hardcoded IP and port.  The recipient will be given a shell running as the current user (apache normally). It can be used to gain access to WordPress sites. 
